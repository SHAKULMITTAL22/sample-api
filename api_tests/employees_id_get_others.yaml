# ********RoostGPT********

# Test generated by RoostGPT for test api-swagger using AI Type Azure Open AI and AI Model Inference
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=0a005b8a0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test1_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_others.csv"
    skipHeader: true
    fields:
      - "id"
    delimiter: "^|^"
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Testing invalid employee ID"
      - get:
          url: "/employees/invalid-uuid"
          headers:
            Authorization: "Bearer {{$processEnvironment.test1_AUTH_TOKEN}}"
      - expect:
          - statusCode: 404
          - hasProperty: error
          - hasProperty: message
          - equals:
              property: "error"
              value: "Not Found"
      - think: 2
      - log: "Testing unauthorized access"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer invalid_token"
      - expect:
          - statusCode: 401
          - hasProperty: error
          - hasProperty: message
          - equals:
              property: "error"
              value: "Unauthorized"
      - think: 2
      - log: "Testing invalid URL"
      - get:
          url: "/invalid-endpoint"
          headers:
            Authorization: "Bearer {{$processEnvironment.test1_AUTH_TOKEN}}"
      - expect:
          - statusCode: 404
          - hasProperty: error
          - hasProperty: message
          - equals:
              property: "error"
              value: "Not Found"
      - think: 2
