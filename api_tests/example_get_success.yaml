# ********RoostGPT********

# Test generated by RoostGPT for test api-swagger using AI Type Azure Open AI and AI Model Inference
# 
# Test generated for /example_get for http method type GET in artillery framework
# RoostTestHash=2d7889707c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test2_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "example_get_success.csv"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Validate successful API responses"
    flow:
      - log: "Starting test for /example endpoint"
      - get:
          url: "/example"
          headers:
            Authorization: "Bearer {{$processEnvironment.test2_AUTH_TOKEN}}"
      - expect:
          - statusCode: 200
          - hasHeader: "Content-Type"
          - hasProperty: "exampleProperty"
          - equals:
              - "exampleProperty"
              - "expectedValue"
      - think: 2
      - log: "Completed test for /example endpoint"
