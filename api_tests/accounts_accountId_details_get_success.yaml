# ********RoostGPT********

# Test generated by RoostGPT for test api-swagger using AI Type Azure Open AI and AI Model Inference
# 
# Test generated for /accounts/{accountId}/details_get for http method type GET in artillery framework
# RoostTestHash=6ef676878c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test4_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_accountId_details_get_success.csv"
    skipHeader: true
    fields:
      - "accountId"
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "Validate successful API responses for /accounts/{accountId}/details"
    flow:
      - log: "Starting GET request to /accounts/{accountId}/details"
      - get:
          url: "/accounts/{{ accountId }}/details"
          headers:
            Authorization: "{{ $processEnvironment.test4_AUTH_TOKEN }}"
            uuid: "12345"
            Accept: "application/json"
            client_id: "client123"
            countryCode: "US"
      - expect:
          - statusCode: 200
          - hasHeader: "Content-Type"
          - hasProperty: "fullName"
          - hasProperty: "firstName"
          - hasProperty: "lastName"
          - hasProperty: "middleName"
          - hasProperty: "localName"
          - hasProperty: "title"
          - hasProperty: "suffix"
          - hasProperty: "maidenName"
          - hasProperty: "companyName"
          - hasProperty: "emails"
          - hasProperty: "emails[0].emailAddress"
          - hasProperty: "emails[0].preferenceType"
          - hasProperty: "addressList"
          - hasProperty: "addressList[0].addressId"
          - hasProperty: "addressList[0].addressLine1"
          - hasProperty: "addressList[0].addressLine2"
          - hasProperty: "addressList[0].addressLine3"
          - hasProperty: "addressList[0].addressType"
          - hasProperty: "addressList[0].city"
          - hasProperty: "addressList[0].countryCode"
          - hasProperty: "addressList[0].postalCode"
          - hasProperty: "addressList[0].state"
          - hasProperty: "phones"
          - hasProperty: "phones[0].areaCode"
          - hasProperty: "phones[0].countryCallingCode"
          - hasProperty: "phones[0].exchangeNumber"
          - hasProperty: "phones[0].extension"
          - hasProperty: "phones[0].fullPhoneNumber"
          - hasProperty: "phones[0].localNumber"
          - hasProperty: "phones[0].phoneType"
          - hasProperty: "phones[0].preferenceType"
      - think: 2
