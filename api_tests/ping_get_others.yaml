# ********RoostGPT********

# Test generated by RoostGPT for test swagger-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /ping_get for http method type GET in artillery framework
# RoostTestHash=628b0897a0
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "ping_get_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses Test Scenario"
    flow:
      - log: "Testing invalid URL scenario: Request API endpoint that does not exist."
      - get:
          url: "/invalid-endpoint"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 404
          - hasHeader: "Content-Type"
          - headerEquals:
              Content-Type: "application/json"
          - notHasProperty: "data"
      - think:
          - duration: 2

      - log: "Testing invalid payload scenario: Send malformed data to the /ping endpoint."
      - post:
          url: "/ping"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          payload:
            invalidKey: "InvalidValue"
      - expect:
          - statusCode: 400
          - hasHeader: "Content-Type"
          - headerEquals:
              Content-Type: "application/json"
          - hasProperty: "error"
          - hasProperty: "message"
          - equals:
              error: "Invalid Request"
      - think:
          - duration: 2

      - log: "Testing missing authorization scenario: Omit Authorization header in the /ping request."
      - get:
          url: "/ping"
          headers:
            Content-Type: "application/json"
      - expect:
          - statusCode: 401
          - hasHeader: "Content-Type"
          - headerEquals:
              Content-Type: "application/json"
          - hasProperty: "error"
          - equals:
              error: "Unauthorized"
      - think:
          - duration: 2

      - log: "Testing unsupported HTTP method: Send a PUT request to /ping endpoint."
      - put:
          url: "/ping"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          payload:
            unexpectedKey: "UnexpectedValue"
      - expect:
          - statusCode: 405
          - hasHeader: "Content-Type"
          - headerEquals:
              Content-Type: "application/json"
          - hasProperty: "error"
          - equals:
              error: "Method Not Allowed"
      - think:
          - duration: 2

      - log: "Testing unsupported media type: Use an invalid Content-Type header."
      - post:
          url: "/ping"
          headers:
            Content-Type: "text/plain"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          payload: "Just a string, not valid JSON."
      - expect:
          - statusCode: 415
          - hasHeader: "Content-Type"
          - headerEquals:
              Content-Type: "application/json"
          - hasProperty: "error"
          - equals:
              error: "Unsupported Media Type"
      - think:
          - duration: 2
