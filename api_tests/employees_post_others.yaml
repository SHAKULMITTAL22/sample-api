# ********RoostGPT********

# Test generated by RoostGPT for test api-swagger using AI Type Azure Open AI and AI Model Inference
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=e5b9d8d67f
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test1_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_post_others.csv"
    skipHeader: true
    fields:
      - "jobTitle"
      - "name"
      - "email"
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Testing invalid URL"
      - get:
          url: "/invalid-endpoint"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals:
                "Content-Type": "application/json; charset=utf-8"
            - hasProperty: "message"
            - equals:
                - "{{ message }}"
                - "Not Found"
      - think: 2
      - log: "Testing invalid payload"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.test1_AUTH_TOKEN}}"
          json:
            jobTitle: "System Analyst"
            name: "John Smith"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals:
                "Content-Type": "application/json; charset=utf-8"
            - hasProperty: "message"
            - equals:
                - "{{ message }}"
                - "Bad Request"
      - think: 2
      - log: "Testing missing required field"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.test1_AUTH_TOKEN}}"
          json:
            jobTitle: "System Analyst"
            email: "john.smith@acme-corp.com"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals:
                "Content-Type": "application/json; charset=utf-8"
            - hasProperty: "message"
            - equals:
                - "{{ message }}"
                - "Bad Request"
      - think: 2
      - log: "Testing invalid authorization"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalid_token"
          json:
            jobTitle: "System Analyst"
            name: "John Smith"
            email: "john.smith@acme-corp.com"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals:
                "Content-Type": "application/json; charset=utf-8"
            - hasProperty: "message"
            - equals:
                - "{{ message }}"
                - "Unauthorized"
      - think: 2
      - log: "Testing invalid content type"
      - post:
          url: "/employees"
          headers:
            Content-Type: "text/plain"
            Authorization: "Bearer {{$processEnvironment.test1_AUTH_TOKEN}}"
          body: "jobTitle=System Analyst&name=John Smith&email=john.smith@acme-corp.com"
          expect:
            - statusCode: 415
            - hasHeader: "Content-Type"
            - headerEquals:
                "Content-Type": "application/json; charset=utf-8"
            - hasProperty: "message"
            - equals:
                - "{{ message }}"
                - "Unsupported Media Type"
