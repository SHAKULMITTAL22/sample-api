# ********RoostGPT********

# Test generated by RoostGPT for test swagger-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=48e4dd6cd8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_get_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: Test non-successful responses for employees API
    flow:
      - log: "Attempting to GET employees with an invalid query parameter."
      - get:
          url: "/employees?searchString=123456789012345678901234567890"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 400
          - hasHeader: "Content-Type"
          - headerEquals:
              header: "Content-Type"
              value: "application/json"
          - matchesRegexp:
              property: "error.code"
              pattern: "EMP_ERR_.*"
      - think: 2

      - log: "Attempting to GET employees with unauthorized access (missing token)."
      - get:
          url: "/employees?searchString=JohnDoe"
          headers:
            Authorization: ""
      - expect:
          - statusCode: 401
          - hasHeader: "Content-Type"
          - headerEquals:
              header: "Content-Type"
              value: "application/json"
          - hasProperty: "error.message"
          - equals:
              property: "error.message"
              value: "Unauthorized access."
      - think: 2

      - log: "Attempting GET request for employees with an invalid URL."
      - get:
          url: "/invalidEmployeesEndpoint?searchString=test"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
          - hasHeader: "Content-Type"
          - headerEquals:
              header: "Content-Type"
              value: "application/json"
          - hasProperty: "error.message"
          - equals:
              property: "error.message"
              value: "The endpoint does not exist."
      - think: 2

      - log: "Attempting to GET employees with malformed query parameter values."
      - get:
          url: "/employees?searchString=<invalidInput>"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 400
          - hasHeader: "Content-Type"
          - headerEquals:
              header: "Content-Type"
              value: "application/json"
          - hasProperty: "error.details"
          - equals:
              property: "error.details"
              value: "Invalid query parameter format."
      - think: 2
