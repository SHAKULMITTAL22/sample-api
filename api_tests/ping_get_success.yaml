# ********RoostGPT********

# Test generated by RoostGPT for test api-swagger using AI Type Azure Open AI and AI Model Inference
# 
# Test generated for /ping_get for http method type GET in artillery framework
# RoostTestHash=628b0897a0
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test2_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "ping_get_success.csv"
    skipHeader: true
    fields:
      - "field1"
      - "field2"
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Ping API Success Scenario"
    flow:
      - log: "Starting Ping API Request"
      - get:
          url: "/ping"
          headers:
            Authorization: "Bearer {{ $processEnvironment.test2_AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
            - headerEquals:
                "Content-Type": "application/json"
      - think: 2
