# ********RoostGPT********

# Test generated by RoostGPT for test api-swagger using AI Type Azure Open AI and AI Model Inference
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=0a005b8a0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test1_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_success.csv"
    skipHeader: true
    fields:
      - "id"
    delimiter: "^|^"

scenarios:
  - name: "Validate Successful API Responses for Employee Endpoint"
    flow:
      - log: "Starting test for getting employee by ID"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.test1_AUTH_TOKEN }}"
          expect:
            - statusCode: 200
      - expect:
          - hasHeader: "Content-Type"
      - expect:
          - headerEquals:
              Content-Type: "application/json; charset=utf-8"
      - expect:
          - hasProperty: "id"
      - expect:
          - hasProperty: "jobTitle"
      - expect:
          - hasProperty: "name"
      - expect:
          - hasProperty: "email"
      - expect:
          - equals:
              - "{{ id }}"
              - "{{ $processEnvironment.test1_AUTH_TOKEN }}"
      - think: 2
