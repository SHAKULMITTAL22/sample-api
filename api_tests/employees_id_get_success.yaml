# ********RoostGPT********

# Test generated by RoostGPT for test swagger-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=0a005b8a0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_success.csv"
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: "Test Successful API Responses for Employee Endpoint"
    flow:
      - log: "Starting scenario to test /employees/{id} endpoint with valid ID."

      - log: "Validating response for GET /employees/{id} with a valid UUID and valid API behavior."

      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      
      - expect:
          - statusCode: 200
          - hasHeader: "Content-Type"
          - headerEquals:
              "Content-Type": "application/json; charset=utf-8"
          - hasProperty: "id"
          - hasProperty: "jobTitle"
          - hasProperty: "name"
          - hasProperty: "email"
          - equals:
              id: "{{ id }}"
          - matchesRegexp:
              jobTitle: "^[\\w\\s]+$"
          - matchesRegexp:
              name: "^[\\w\\s]+$"
          - matchesRegexp:
              email: "^[\\w.-]+@[\\w.-]+\\.[A-Za-z]{2,4}$"

      - think: 2
