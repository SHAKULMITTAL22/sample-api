# ********RoostGPT********

# Test generated by RoostGPT for test swagger-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=48e4dd6cd8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: employees_get_success.csv
    delimiter: "^|^"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: Test Employees API with Successful Responses
    flow:
      - log:
          message: "Starting test: Validate GET /employees endpoint with optional filters"
      - get:
          url: "/employees?searchString={{ searchString }}"
          headers: 
            Content-Type: application/json
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 200
          - contentType: application/json
          - hasProperty:
              - type: array
      - expect:
          - hasProperty:
              - items
      - think:
          duration: 2
