# ********RoostGPT********

# Test generated by RoostGPT for test swagger-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=e5b9d8d67f
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_post_success.csv"
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: Successful Employee API Response Scenarios
    flow:
      - log: "Testing valid POST request for 'Add Employee' endpoint."
      - post:
          url: "/employees"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
            email: "{{ email }}"
      - expect:
          - statusCode: 201
          - contentType: "application/json"
          - hasProperty: "id"
          - hasProperty: "jobTitle"
          - hasProperty: "name"
          - hasProperty: "email"
          - matchesRegexp:
              property: "email"
              pattern: "^[\\w-.]+@[\\w]+\\.[\\w]{2,4}$"
      - think: 2
