# ********RoostGPT********

# Test generated by RoostGPT for test api-swagger using AI Type Azure Open AI and AI Model Inference
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=e5b9d8d67f
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test1_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_post_success.csv"
    fields:
      - "jobTitle"
      - "name"
      - "email"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Validate successful employee creation"
    flow:
      - log: "Starting employee creation request"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.test1_AUTH_TOKEN}}"
          json:
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeader: "Content-Type"
            - headerEquals:
                header: "Content-Type"
                value: "application/json"
            - hasProperty: "id"
            - hasProperty: "jobTitle"
            - hasProperty: "name"
            - hasProperty: "email"
      - think: 2
